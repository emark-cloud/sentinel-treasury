# Secrets
.env
.env.local
keys/
*.pem
*secret_key*

# Node
node_modules/
.pnpm-store/
dist/
.next/
*.log
*.tsbuildinfo
next-env.d.ts

# Rust / Odra
target/
wasm/
*.wasm
# …except Odra's vendored proxy_caller shim: a runtime asset the dashboard ships to fund the
# payable deposit_cspr call (fetched at /proxy_caller.wasm), not a build artifact.
!apps/dashboard/public/proxy_caller.wasm
**/*.rs.bk

# OS / editor
.DS_Store
.idea/
.vscode/
