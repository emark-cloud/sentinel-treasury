# Odra contract registry. Build order: AuditLog first (the vault's init takes its address),
# then SentinelVault. Both are upgradable Casper contract packages.

[[contracts]]
fqn = "sentinel_contracts::audit_log::AuditLog"

[[contracts]]
fqn = "sentinel_contracts::vault::SentinelVault"
